/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
// This configuration file sets up Tailwind CSS for your project.
// It specifies the content files where Tailwind's utility classes will be applied,
// extends the default theme, and allows you to add custom plugins if needed.